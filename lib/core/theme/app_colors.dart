import 'package:flutter/material.dart';

class AppColors{
  //light mode
  //static const primary = Color(0xFF03DAC6);
  static const primary = Colors.blue;
  static const primaryShade300 = Color.fromRGBO(100, 181, 246, 1);
  static const secondary = Colors.blueGrey;
  static const background = Colors.white;
  static const textPrimary = Colors.black;
  static const textPrimary2 = Colors.black54;
  static const textPrimary3 = Colors.black45;
  static const textSecondary = Colors.grey;
  static const bgCard = Color.fromRGBO(224, 224, 224, 1);
  static const inherit = Colors.transparent;
  static const textError = Colors.red;
  static const textSuccess = Colors.green;
  //dark mode
  static const primaryDark = Color.fromRGBO(255, 255, 255, 1);
  static const secondaryDark = Color.fromRGBO(255, 255, 255, 0.8);
  static const backgroundDark = Color(0xFF000000);
  static const textPrimaryDark = Color(0xFFF5F5F5);
  static const textSecondaryDark = Color(0xFF666666);

  //snackbar
  static const snackbarSuccess = Colors.green;
  static const snackbarError = Colors.red;
  static const snackbarWarning = Colors.orange;
  static const snackbarInfo = Colors.blue;
  static const snackbarTextSuccess = Colors.white;
  static const snackbarTextError = Colors.black;
  static const snackbarTextWarning = Colors.black;
  static const snackbarTextInfo = Colors.white;
}



